---
import { Image } from 'astro:assets'

import { PostPreview } from 'astro-pure/components/pages'
import { getBlogCollection, sortMDByDate } from 'astro-pure/server'
import { Button, Card, Icon, Label } from 'astro-pure/user'
import PageLayout from '@/layouts/BaseLayout.astro'
import Section from '@/components/home/Section.astro'
import SkillLayout from '@/components/home/SkillLayout.astro'
import config from '@/site-config'

const skillGroups = [
  {
    title: 'LLM / Agent',
    skills: ['Dify', 'Workflow', 'MCP', 'RAG', 'Prompt Engineering', 'Langfuse']
  },
  {
    title: 'Modeling',
    skills: ['PyTorch', 'Transformers', 'ALBEF', 'CLIP', 'DDP', 'LoRA']
  },
  {
    title: 'Engineering',
    skills: ['Python', 'FastAPI', 'Docker Compose', 'Linux', 'Shell', 'Git']
  },
  {
    title: 'Web',
    skills: ['Astro', 'TypeScript', 'Vercel', 'Markdown']
  }
]

const MAX_POSTS = 10
const allPosts = await getBlogCollection()
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS)

const imagePath = config.logo.src
const images = import.meta.glob<{ default: ImageMetadata }>(
  '/src/assets/*.{jpeg,jpg,png,gif,avif,webp}'
)
if (!images[imagePath])
  throw new Error(
    `"${imagePath}" does not exist in glob: "src/assets/*.{jpeg,jpg,png,gif,avif,webp}"`
  )
---

<PageLayout meta={{ title: 'Home' }} highlightColor='#659EB9'>
  <main class='flex w-full flex-col items-center'>
    <section
      class='animate mb-10 flex flex-col items-center gap-y-7 text-center'
      id='content-header'
    >
      <Image
        src={images[imagePath]()}
        alt={config.logo.alt || 'Profile'}
        class='h-28 w-28 rounded-3xl border bg-background p-1 shadow-sm'
        loading='eager'
        fetchpriority='high'
      />

      <div class='flex flex-col items-center gap-y-4'>
        <div class='flex flex-col items-center gap-y-2'>
          <p
            class='inline-flex items-center gap-x-2 rounded-full border bg-muted px-3 py-1 text-sm font-medium text-muted-foreground'
          >
            <span class='relative flex size-2'>
              <span
                class='absolute inline-flex h-full w-full animate-ping rounded-full bg-green-400 opacity-75'
              ></span>
              <span class='relative inline-flex size-2 rounded-full bg-green-500'></span>
            </span>
            I am alive
          </p>
          <h1 class='text-3xl font-medium sm:text-4xl'>{config.title}</h1>
          <p class='max-w-2xl text-muted-foreground'>
            这里会放技术折腾、成长记录和一些还没想明白但值得写下来的问题。最近主要在玩 LLM / Agent /
            RAG，也会记录多模态检索、Linux、Astro 博客和 AI 工具链里的坑。
          </p>
        </div>
        <div class='flex flex-wrap justify-center gap-x-7 gap-y-3'>
          <Label title='武汉'>
            <Icon name='location' class='size-5' slot='icon' />
          </Label>
          <Label title='cxdpowered' as='a' href='https://github.com/cxdpowered' target='_blank'>
            <Icon name='github' class='size-5' slot='icon' />
          </Label>
          <Label title='1293909048@qq.com' as='a' href='mailto:1293909048@qq.com'>
            <Icon name='email' class='size-5' slot='icon' />
          </Label>
        </div>
      </div>

      <div class='flex flex-wrap justify-center gap-3'>
        <Button title='Read posts' href='/blog' variant='ahead' />
        <Button title='About me' href='/about' variant='ahead' />
      </div>
    </section>

    <div id='content' class='animate flex flex-col gap-y-10 md:w-4/5 lg:w-5/6'>
      <Section title='Profile'>
        <p class='text-muted-foreground'>
          比起只把结果贴出来，我更想记录过程：为什么这样选型，哪里踩坑，哪些想法后来被推翻。
        </p>
        <p class='text-muted-foreground'>
          这个博客也会顺手留下能证明技术能力的痕迹，但它首先是我的长期记录：把学习、研究、项目和生活里的小发现写得有趣一点。
        </p>
      </Section>

      <Section title='Now'>
        <div class='grid grid-cols-1 gap-3 sm:grid-cols-2'>
          <Card
            heading='LLM Agent 工程'
            subheading='function calling 工具循环、长期记忆、MCP 工具化、多平台 IM 接入'
          />
          <Card
            heading='多模态检索研究'
            subheading='ALBEF、弱监督跨模态对齐、PyTorch / DDP 训练与复现'
          />
        </div>
      </Section>

      <Section title='Projects'>
        <Card
          heading='小青团 · xiaoqingtuan'
          subheading='面向 QQ / 微信的个人任务型 LLM Agent 助手'
          date='2026.05 - 至今'
        >
          <p class='text-muted-foreground'>
            对话与任务执行共用同一套 function calling
            工具循环，闲聊和真正「办事」走同一条路径；Wiki-first 长期记忆配 BM25 +
            语义混合检索，还能调用外部 MCP 服务（如下方图书馆选座）。
          </p>
        </Card>
        <Card
          heading='mcp-ccnu-lib'
          subheading='把华师图书馆选座系统封装成模型可调用的 MCP 工具'
          date='2026.06'
        >
          <p class='text-muted-foreground'>
            覆盖登录、查座、预约、取消全流程，基于 Playwright
            维持持久会话；验证码用「挂起返回」交回用户输入，不阻塞流程，决策与调度交给小青团。
          </p>
        </Card>
        <Card
          heading='个人知识发布 Agent'
          subheading='基于 Dify 的知识沉淀与技术内容发布闭环'
          date='2026.04 - 至今'
        >
          <p class='text-muted-foreground'>
            把对话片段、笔记等碎片沉淀为长期记忆，经 RAG 召回生成博客草稿，再接 MCP 发布工具链完成
            frontmatter 校验、死链检查到 Astro / Vercel 部署的发布闭环。
          </p>
        </Card>
        <Button
          title='More project details'
          class='w-fit self-end'
          href='/projects'
          variant='ahead'
        />
      </Section>

      <Section title='Education'>
        <Card
          heading='华中师范大学 | 计算机学院'
          subheading='专业硕士研究生，视觉-文本跨模态方向'
          date='2024.09 - 2027.06（预计）'
        />
        <Card
          heading='湖北经济学院 | 物流工程'
          subheading='工学学士，本科后有一年配送业务一线执行经历'
          date='2017.09 - 2021.06'
        />
      </Section>

      <Section title='Practice'>
        <Card heading='多模态情感计算小组' subheading='服务器运维与研发支持' date='研究生阶段'>
          <p class='text-muted-foreground'>
            负责训练服务器组装、Linux / CUDA 环境配置、VPN 与权限维护，并用 Shell
            脚本处理环境初始化、日志清理和结果归档。
          </p>
        </Card>
        <Card
          heading='WPS 菁英工程师培养计划'
          subheading='C++ / Qt 方向，8 周结业认证'
          date='2026.03 - 2026.05'
        />
      </Section>

      <Section title='Skills'>
        {skillGroups.map((group) => <SkillLayout title={group.title} skills={group.skills} />)}
      </Section>

      {
        allPostsByDate.length > 0 && (
          <Section title='Posts'>
            <ul class='flex flex-col gap-y-1.5 sm:gap-y-2'>
              {allPostsByDate.map((p) => (
                <li class='flex flex-col gap-x-2 sm:flex-row'>
                  <PostPreview post={p} />
                </li>
              ))}
            </ul>
            <Button title='More posts' class='w-fit self-end' href='/blog' variant='ahead' />
          </Section>
        )
      }
    </div>
  </main>
</PageLayout>
